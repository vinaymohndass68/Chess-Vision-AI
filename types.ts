export enum PlayerSide {
  White = 'white',
  Black = 'black',
}

export enum AppState {
  SelectingSide = 'SELECTING_SIDE',
  Camera = 'CAMERA',
  Loading = 'LOADING',
  Result = 'RESULT',
  Error = 'ERROR',
}